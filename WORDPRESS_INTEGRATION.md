# WordPress API Integration - Руководство по установке

## 📋 Обзор

Интеграция позволяет Telegram боту получать информацию о записях в реальном времени из WordPress и показывать пользователям только свободные временные слоты.

## ✅ Что уже сделано

1. ✅ **WordPress API клиент** (`wordpress_api.py`)

   - Автоматическое определение плагина для записи
   - Получение записей из WordPress
   - Вычисление свободных слотов
   - Создание новых записей через API

2. ✅ **Интеграция с ботом** (`clinic_bot.py`)

   - Показ только свободных временных слотов
   - Отображение количества занятых/свободных слотов
   - Синхронизация записей с WordPress
   - Обработка ошибок API

3. ✅ **Конфигурация** (`config.py`)

   - Настройки WordPress API
   - Параметры рабочего времени
   - Длительность приема

4. ✅ **Тестирование**
   - Скрипт проверки подключения (`test_wordpress_api.py`)
   - Подключение к diason.uz работает ✅

## 🔧 Установка WordPress плагина

### Шаг 1: Загрузка плагина на сервер

Скопируйте файл `wordpress-plugin/clinic-telegram-bot-api.php` на ваш WordPress сервер:

```bash
# Через FTP/SFTP загрузите файл в:
/wp-content/plugins/clinic-telegram-bot-api/clinic-telegram-bot-api.php
```

### Шаг 2: Активация плагина

1. Войдите в админ-панель WordPress (https://diason.uz/wp-admin)
2. Перейдите в **Плагины** → **Установленные плагины**
3. Найдите **Clinic Telegram Bot API**
4. Нажмите **Активировать**

### Шаг 3: Проверка таблиц

После активации плагин автоматически создаст таблицы:

- `wp_doctors` - таблица врачей
- `wp_appointments` - таблица записей

Проверить можно через phpMyAdmin или SQL запрос:

```sql
SHOW TABLES LIKE 'wp_doctors';
SHOW TABLES LIKE 'wp_appointments';
```

## 🚀 Запуск бота

### 1. Установка зависимостей

```bash
cd c:\Users\tshst\Documents\clinic_tg_bot
pip install -r requirements.txt
```

### 2. Проверка подключения к WordPress

```bash
python test_wordpress_api.py
```

Вы должны увидеть:

```
✅ WordPress URL: https://diason.uz
✅ Плагин: standard
✅ API работает: Да
```

### 3. Запуск бота

```bash
python clinic_bot.py
```

Или через bat файл:

```bash
run_bot.bat
```

## 📱 Как работает бот

### Процесс записи:

1. **Пользователь**: `/book` - начинает запись
2. **Бот**: Показывает список врачей из БД
3. **Пользователь**: Выбирает врача
4. **Бот**: Показывает доступные даты (7 дней вперед)
5. **Пользователь**: Выбирает дату
6. **Бот**:
   - 🔄 Запрашивает занятые слоты из WordPress API
   - 🧮 Вычисляет свободные слоты
   - ✅ Показывает ТОЛЬКО свободные времена
   - ❌ Показывает количество занятых слотов
7. **Пользователь**: Выбирает время
8. **Пользователь**: Вводит ФИО и телефон
9. **Бот**:
   - 💾 Сохраняет запись в локальную БД
   - 🔄 Синхронизирует с WordPress через API
   - ✅ Подтверждает запись

### Пример отображения:

```
📅 Выберите время приёма на 2025-12-30:

✅ Свободных слотов: 14
❌ Занятых слотов: 2

Занятые времена: 10:00, 14:30

[✅ 09:00] [✅ 09:30] [✅ 10:30]
[✅ 11:00] [✅ 11:30] [✅ 12:00]
...
```

## 🔍 API Endpoints

Плагин создает следующие endpoints:

### 1. Получение записей

```
GET https://diason.uz/wp-json/clinic/v1/get-appointments
```

Параметры:

- `doctor_id` (опционально) - ID врача
- `date` (опционально) - Дата в формате YYYY-MM-DD

Пример:

```bash
curl -u username:password \
  "https://diason.uz/wp-json/clinic/v1/get-appointments?doctor_id=1&date=2025-12-30"
```

Ответ:

```json
[
  {
    "id": 1,
    "doctor_id": 1,
    "date": "2025-12-30",
    "time": "10:00",
    "patient_name": "Иванов Иван",
    "patient_phone": "+998901234567",
    "status": "confirmed"
  }
]
```

### 2. Создание записи

```
POST https://diason.uz/wp-json/clinic/v1/appointments
```

Тело запроса:

```json
{
  "doctor_id": 1,
  "appointment_date": "2025-12-30",
  "appointment_time": "15:00",
  "user_name": "Петров Петр",
  "user_phone": "+998901234567"
}
```

### 3. Получение врачей

```
GET https://diason.uz/wp-json/clinic/v1/doctors
```

Публичный endpoint, не требует авторизации.

## ⚙️ Настройки

### config.py

```python
# WordPress API
WORDPRESS_CONFIG = {
    'enabled': True,  # Включить/выключить интеграцию
    'site_url': 'https://diason.uz',
    'username': 's1143023_da5on46',
    'password': 'BZ64^A1Tw*&n',
    'verify_ssl': True,
    'timeout': 10,
}

# Рабочее время
WORKING_HOURS = {
    'start': '09:00',
    'end': '18:00',
    'lunch_start': '13:00',
    'lunch_end': '14:00'
}

# Длительность приема (минуты)
APPOINTMENT_DURATION = 30
```

## 🐛 Решение проблем

### Проблема: "WordPress API недоступен"

**Решение:**

1. Проверьте, что сайт доступен: `ping diason.uz`
2. Проверьте учетные данные в `config.py`
3. Убедитесь, что плагин активирован в WordPress
4. Попробуйте отключить `verify_ssl`:
   ```python
   'verify_ssl': False
   ```

### Проблема: "Плагин не определен"

**Решение:**
Это нормально! Бот использует стандартный API через custom endpoints.
Убедитесь, что плагин `clinic-telegram-bot-api.php` активирован.

### Проблема: "Все слоты показываются как свободные"

**Решение:**

1. Проверьте, что в БД есть записи:
   ```sql
   SELECT * FROM wp_appointments WHERE appointment_date = '2025-12-30';
   ```
2. Проверьте логи бота: `clinic_bot.log`
3. Запустите тест: `python test_wordpress_api.py`

### Проблема: "Запись не синхронизируется с WordPress"

**Решение:**

1. Проверьте логи бота на ошибки синхронизации
2. Убедитесь, что у пользователя есть права на создание записей
3. Проверьте, что endpoint `/wp-json/clinic/v1/appointments` доступен

## 📊 Мониторинг

### Проверка статуса системы

В боте используйте команду:

```
/status
```

Вы увидите:

- Количество врачей в БД
- Количество записей
- Статус подключения

### Логи

Все действия записываются в:

- `clinic_bot.log` - логи бота
- WordPress Debug Log (если включен)

## 🔐 Безопасность

1. **Авторизация**: Все API запросы используют Basic Auth
2. **Валидация**: Проверка всех входных данных
3. **Проверка слотов**: Предотвращение двойной записи
4. **SSL**: Рекомендуется использовать HTTPS

## 📝 Дополнительная информация

### Структура проекта

```
clinic_tg_bot/
├── clinic_bot.py              # Основной файл бота
├── wordpress_api.py           # WordPress API клиент
├── config.py                  # Конфигурация
├── test_wordpress_api.py      # Тесты API
├── requirements.txt           # Зависимости
├── run_bot.bat               # Запуск бота
└── wordpress-plugin/
    └── clinic-telegram-bot-api.php  # WordPress плагин
```

### Команды бота

- `/start` - Начало работы
- `/book` - Запись на прием
- `/doctors` - Список врачей
- `/info` - О клинике
- `/status` - Статус системы
- `/help` - Помощь

## 🎯 Следующие шаги

1. ✅ Загрузите и активируйте WordPress плагин
2. ✅ Запустите тест: `python test_wordpress_api.py`
3. ✅ Запустите бота: `python clinic_bot.py`
4. ✅ Протестируйте запись через бота
5. ✅ Проверьте, что запись появилась в WordPress БД

## 💡 Поддержка

При возникновении проблем:

1. Проверьте логи: `clinic_bot.log`
2. Запустите тест: `python test_wordpress_api.py`
3. Проверьте конфигурацию: `config.py`
