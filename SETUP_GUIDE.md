# 🎯 ПОШАГОВАЯ ИНСТРУКЦИЯ - ЧТО ДЕЛАТЬ СЕЙЧАС

## ⚡ БЫСТРЫЙ СТАРТ (5 минут)

### ШАГ 1: Установите WordPress плагин (2 минуты)

У вас есть файл плагина здесь:

```
c:\Users\tshst\Documents\clinic_tg_bot\wordpress-plugin\clinic-telegram-bot-api.php
```

**Вариант А - Через FTP/FileZilla (рекомендуется):**

1. Откройте FileZilla или другой FTP клиент
2. Подключитесь к серверу diason.uz
3. Перейдите в папку: `/public_html/wp-content/plugins/`
4. Создайте новую папку: `clinic-telegram-bot-api`
5. Загрузите файл `clinic-telegram-bot-api.php` в эту папку

**Вариант Б - Через cPanel:**

1. Войдите в cPanel вашего хостинга
2. Откройте "Файловый менеджер" (File Manager)
3. Перейдите в: `public_html/wp-content/plugins/`
4. Нажмите "Создать папку" → введите `clinic-telegram-bot-api`
5. Войдите в эту папку
6. Нажмите "Загрузить" → выберите файл `clinic-telegram-bot-api.php`

**Активация плагина:**

1. Откройте браузер → https://diason.uz/wp-admin
2. Войдите (username: s1143023_da5on46)
3. Меню слева → **Плагины** → **Установленные плагины**
4. Найдите **Clinic Telegram Bot API**
5. Нажмите **Активировать**

✅ **Готово!** Плагин автоматически создаст нужные таблицы.

---

### ШАГ 2: Проверьте подключение (1 минута)

Откройте PowerShell или командную строку:

```powershell
cd c:\Users\tshst\Documents\clinic_tg_bot
python test_wordpress_api.py
```

**Ожидаемый результат:**

```
✅ Подключено к: Diason - клиника
✅ Плагин: standard
✅ API работает: Да
✅ Свободных слотов: 16
```

Если видите это - всё работает! ✅

---

### ШАГ 3: Запустите бота (1 минута)

**Способ 1 - Через bat файл:**
Двойной клик на файл: `run_bot.bat`

**Способ 2 - Через командную строку:**

```powershell
cd c:\Users\tshst\Documents\clinic_tg_bot
python clinic_bot.py
```

**Вы увидите:**

```
🏥 БОТ ДЛЯ ЗАПИСИ В КЛИНИКУ
База данных: s1143023_da5on46
Префикс таблиц: wp_
Бот запущен. Нажмите Ctrl+C для остановки.
```

✅ **Бот работает!**

---

### ШАГ 4: Протестируйте в Telegram (1 минута)

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте: `/book`
4. Выберите врача
5. Выберите дату (например, завтра)
6. **Смотрите результат!** 🎉

**Вы увидите:**

```
📅 Выберите время приёма на 2025-12-31:

✅ Свободных слотов: 16
❌ Занятых слотов: 0

[✅ 09:00] [✅ 09:30] [✅ 10:00]
[✅ 10:30] [✅ 11:00] [✅ 11:30]
...
```

---

## 🧪 ТЕСТ: Проверка работы занятых слотов

Чтобы убедиться, что бот правильно показывает занятые слоты:

### Вариант 1 - Через phpMyAdmin:

1. Откройте phpMyAdmin
2. Выберите базу: `s1143023_da5on46`
3. Откройте таблицу: `wp_appointments`
4. Нажмите "Вставить" (Insert)
5. Заполните:
   ```
   doctor_id: 1
   appointment_date: 2025-12-31
   appointment_time: 10:00:00
   user_name: Тестовая запись
   user_phone: +998901234567
   status: confirmed
   ```
6. Нажмите "Вперёд" (Go)

### Вариант 2 - Через SQL запрос:

```sql
INSERT INTO wp_appointments
(doctor_id, appointment_date, appointment_time, user_name, user_phone, status, created_at)
VALUES
(1, '2025-12-31', '10:00:00', 'Тестовая запись', '+998901234567', 'confirmed', NOW());
```

### Проверка в боте:

1. Откройте бота в Telegram
2. `/book` → выберите врача → выберите 31 декабря
3. **Время 10:00 НЕ должно показываться!** ✅
4. Вы увидите:
   ```
   ✅ Свободных слотов: 15
   ❌ Занятых слотов: 1
   Занятые времена: 10:00
   ```

---

## ⚙️ НАСТРОЙКИ (если нужно изменить)

Откройте файл: `c:\Users\tshst\Documents\clinic_tg_bot\config.py`

### Изменить рабочее время:

```python
WORKING_HOURS = {
    'start': '09:00',      # Начало работы
    'end': '18:00',        # Конец работы
    'lunch_start': '13:00', # Начало обеда
    'lunch_end': '14:00'   # Конец обеда
}
```

### Изменить длительность приема:

```python
APPOINTMENT_DURATION = 30  # минуты (30 = полчаса)
```

Если хотите приемы по часу:

```python
APPOINTMENT_DURATION = 60  # минуты
```

### Отключить WordPress API (если нужно):

```python
WORDPRESS_CONFIG = {
    'enabled': False,  # Изменить на False
    ...
}
```

---

## 🔍 ПРОВЕРКА СТАТУСА

В любой момент можете проверить статус системы:

**В боте:**

```
/status
```

**Результат:**

```
📊 Статус системы

✅ База данных: s1143023_da5on46
✅ Префикс таблиц: wp_
👨‍⚕️ Врачей в базе: X
📅 Всего записей: Y
🤖 Бот работает: Да
```

---

## 📁 СТРУКТУРА ФАЙЛОВ

```
clinic_tg_bot/
├── clinic_bot.py              ← Основной бот (запускать этот)
├── wordpress_api.py           ← WordPress клиент
├── config.py                  ← Настройки (редактировать здесь)
├── test_wordpress_api.py      ← Тест подключения
├── run_bot.bat               ← Быстрый запуск бота
├── QUICK_START.md            ← Быстрый старт
├── WORDPRESS_INTEGRATION.md  ← Полное руководство
└── wordpress-plugin/
    └── clinic-telegram-bot-api.php  ← Загрузить на сервер
```

---

## 🐛 ЧТО ДЕЛАТЬ ЕСЛИ...

### "Бот не запускается"

```powershell
pip install -r requirements.txt
```

### "WordPress API недоступен"

1. Проверьте, что плагин активирован в WordPress
2. Запустите тест:
   ```powershell
   python test_wordpress_api.py
   ```
3. Проверьте логи: `clinic_bot.log`

### "Все слоты показываются как свободные"

1. Убедитесь, что плагин активирован
2. Проверьте, есть ли записи в БД:
   ```sql
   SELECT * FROM wp_appointments WHERE appointment_date >= CURDATE();
   ```

### "Нужна помощь"

Проверьте файл логов:

```
c:\Users\tshst\Documents\clinic_tg_bot\clinic_bot.log
```

---

## ✅ ЧЕКЛИСТ

- [ ] Загрузил плагин на сервер WordPress
- [ ] Активировал плагин в админ-панели
- [ ] Запустил тест: `python test_wordpress_api.py`
- [ ] Увидел "✅ API работает: Да"
- [ ] Запустил бота: `python clinic_bot.py`
- [ ] Протестировал в Telegram: `/book`
- [ ] Увидел свободные слоты
- [ ] Создал тестовую запись в БД
- [ ] Проверил, что занятое время не показывается

---

## 🎉 ГОТОВО!

После выполнения всех шагов ваш бот будет:

- ✅ Показывать только свободные слоты
- ✅ Синхронизировать записи с WordPress
- ✅ Предотвращать двойные записи
- ✅ Работать в реальном времени

**Начните с ШАГ 1!** 🚀

---

## 📞 КОМАНДЫ БОТА

- `/start` - Приветствие
- `/book` - **Запись на прием** ⭐
- `/doctors` - Список врачей
- `/status` - Статус системы
- `/info` - О клинике
- `/help` - Помощь
